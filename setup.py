from setuptools import setup

setup(
  name = 'asciigraphics',
  version = '0.0.1',
  description = 'Do Pixel Graphics with ascii charecters',
  py_modules = ["asciianim"],
  package_dir = {'':'src'},
  )
